[build]
  base = "frontend"
  publish = "frontend/build"
  command = "npm ci && npm run build"

[[redirects]]
  from = "/api/*"
  to = "https://viv-deploy.onrender.com/:splat"
  status = 200
  force = true

